import { EstadoVehiculoPipe } from './estado-vehiculo.pipe';

describe('EstadoVehiculoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoVehiculoPipe();
    expect(pipe).toBeTruthy();
  });
});
