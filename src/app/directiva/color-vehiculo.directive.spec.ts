import { ColorVehiculoDirective } from './color-vehiculo.directive';

describe('ColorVehiculoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorVehiculoDirective();
    expect(directive).toBeTruthy();
  });
});
